"""Escreva uma função que receba dois números inteiros e retorne o maior dos mesmos.

Input
A função recebe como parâmetros 2 (dois) números inteiros a e b ( - 1000 ≤ a, b ≤ 1000).

Assinatura da função em Python:

def maior2(a,b)

Importante: O código que você enviar não deve mostrar nada na tela (função print()). Deve conter apenas a implementação da função.

Output
A função deve retornar um valor inteiro, o maior número entre a e b."""

def maior2(a,b):
    if(a>b):
        return a 
    else:
        return b